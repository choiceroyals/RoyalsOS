# Business Research Starter

This is a safe RoyalOS V3 declarative plugin example. Upload the ZIP from **Plugins → Upload Plugin ZIP**. It registers an Atlas research capability and a runnable report action without copying code into `app`, `components`, or `lib`.

It requires RoyalOS core AI configuration to execute the report. The package contains no JavaScript and cannot run terminal commands.
